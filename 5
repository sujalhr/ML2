from minisom import MiniSom
from sklearn.datasets import load_iris
import matplotlib.pyplot as plt

data = load_iris().data
som = MiniSom(10,10,4); som.train_random(data,100)
u = som.distance_map()
plt.imshow(u, cmap='bone')
plt.colorbar()
plt.show()
